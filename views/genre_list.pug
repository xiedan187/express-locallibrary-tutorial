extends layout 

block content 
    h1=title 
    if genre_list.length 
        ul 
            each item in genre_list
                li 
                    a(href=item.url) #{item.name}
    else 
        p 数据为空